import React from 'react'

export const Scores = (props) => {
  return(
    <div>
      <h2>Your Childs Score (Each child with their score can be listed here)</h2>
    </div>
  )
}

export default Scores